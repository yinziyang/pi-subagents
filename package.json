{
  "name": "pi-subagents",
  "version": "0.1.0",
  "description": "Claude Code–style subagents for the pi coding agent: isolated contexts, background runs, forks, resume, and a live agent panel.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "repository": "github:yinziyang/pi-subagents",
  "keywords": [
    "pi-package",
    "pi",
    "pi-coding-agent",
    "subagents",
    "claude-code"
  ],
  "files": [
    "extensions",
    "agents",
    "README.md"
  ],
  "pi": {
    "extensions": [
      "./extensions/subagents/index.ts"
    ]
  },
  "scripts": {
    "test": "node --test \"test/*.test.ts\"",
    "typecheck": "npx -y -p typescript@5 tsc --noEmit --strict --target es2022 --module nodenext --moduleResolution nodenext --allowImportingTsExtensions --skipLibCheck extensions/subagents/*.ts extensions/subagents/ui/*.ts",
    "e2e": "node test/e2e/e2e.mjs && node test/e2e/rpc.mjs"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.87.0",
    "@earendil-works/pi-coding-agent": "^0.87.0",
    "typebox": "^1.3.27"
  }
}
